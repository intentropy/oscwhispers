# Configure the server for incoming OSC messages
server:
  port: 9000

# Define forwarding routes
routes:
  pathname:
    - host: 127.0.0.2
      port: 9003
      truncate: no
    - host: 127.0.0.1
      port: 9001
      truncate: True
  otherpathname:
    - host: 127.0.0.1
      port: 9002